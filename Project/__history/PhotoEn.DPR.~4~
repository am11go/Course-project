program PhotoEn;

uses
  Forms,
  Main in 'Main.pas' {MainForm},
  About in 'About.pas' {PAbout},
  uCloseTabSheet in 'uCloseTabSheet.pas',
  Loading in 'Loading.pas' {Form1};

{$R *.RES}

begin
  Application.Title := 'PhotoEn - ImageEn Demo';
  Application.CreateForm(TMainForm, MainForm);
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.

